/*
 * The Jira Cloud platform REST API
 *
 * Jira Cloud platform REST API documentation
 *
 * The version of the OpenAPI document: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 * Generated by: https://openapi-generator.tech
 */

use std::borrow::Borrow;
#[allow(unused_imports)]
use std::option::Option;
use std::rc::Rc;

use reqwest;

use super::{configuration, Error};

pub struct ApplicationRolesApiClient {
    configuration: Rc<configuration::Configuration>,
}

impl ApplicationRolesApiClient {
    pub fn new(configuration: Rc<configuration::Configuration>) -> ApplicationRolesApiClient {
        ApplicationRolesApiClient { configuration }
    }
}

pub trait ApplicationRolesApi {
    fn get_all_application_roles(&self) -> Result<Vec<crate::models::ApplicationRole>, Error>;
    fn get_application_role(&self, key: &str) -> Result<crate::models::ApplicationRole, Error>;
}

impl ApplicationRolesApi for ApplicationRolesApiClient {
    fn get_all_application_roles(&self) -> Result<Vec<crate::models::ApplicationRole>, Error> {
        let configuration: &configuration::Configuration = self.configuration.borrow();
        let client = &configuration.client;

        let uri_str = format!("{}/rest/api/3/applicationrole", configuration.base_path);
        let mut req_builder = client.get(uri_str.as_str());

        if let Some(ref user_agent) = configuration.user_agent {
            req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
        }
        if let Some(ref auth_conf) = configuration.basic_auth {
            req_builder = req_builder.basic_auth(auth_conf.0.to_owned(), auth_conf.1.to_owned());
        };

        // send request
        let req = req_builder.build()?;

        Ok(client.execute(req)?.error_for_status()?.json()?)
    }

    fn get_application_role(&self, key: &str) -> Result<crate::models::ApplicationRole, Error> {
        let configuration: &configuration::Configuration = self.configuration.borrow();
        let client = &configuration.client;

        let uri_str = format!(
            "{}/rest/api/3/applicationrole/{key}",
            configuration.base_path,
            key = crate::apis::urlencode(key)
        );
        let mut req_builder = client.get(uri_str.as_str());

        if let Some(ref user_agent) = configuration.user_agent {
            req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
        }
        if let Some(ref auth_conf) = configuration.basic_auth {
            req_builder = req_builder.basic_auth(auth_conf.0.to_owned(), auth_conf.1.to_owned());
        };

        // send request
        let req = req_builder.build()?;

        Ok(client.execute(req)?.error_for_status()?.json()?)
    }
}
